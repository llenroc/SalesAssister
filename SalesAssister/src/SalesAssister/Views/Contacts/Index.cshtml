@{
    ViewData["Title"] = "Sales Assister";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using System.Security.Claims

@model SalesAssister.Models.Client

<p>@Html.ActionLink("BACK TO CLIENT LIST", "Index", "Clients")</p>

<h2 class="salesPeople">Notes for @Model.Name</h2>

@if (ViewBag.Contacts != null)
{
    @foreach (var x in ViewBag.Contacts)
    {
        <hr />
        <h4>@x.Date.ToString("MM/dd/yyyy")</h4>
        <p>@Html.ActionLink("EDIT", "Edit", new { id = @x.ContactId })</p>
        <ul>
            <li>@Html.Raw(@x.Notes.Replace("\n", "<br />"))</li>
        </ul>
    }
}
else if (ViewBag.Count == 0)
{
    <p>test</p>
}

@using (Html.BeginForm("addNotes", "Contacts"))
{
    <h3>Add Contact Notes</h3>

    <input type="hidden" name="ClientId" value="@Model.ClientId" />

    <div>Meeting Notes</div>
    <textarea class="textBox" type="text" name="Notes" value="@ViewBag.Notes"></textarea>

    <div>Date</div>
    <input type="date" name="Date" value="@ViewBag.Date"/>

    <br />
    <br />
    <div>
        <input type="submit" value="Save" class="btn btn-warning" />
    </div>
}
